program programMax
global
  const x:int64;
  const y:int64

  proc max(a:int64, b:int64)
  do
    if a > b then
      debugout a
    else
      debugout b
    endif
  endproc
  
do
  // test right / left asso
  debugin x init;
  debugin y init;
  max(x, y)
endprogram